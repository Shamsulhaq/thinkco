// Stub for ink's optional dev dependency `react-devtools-core` (only used in DEV).
// Bundled production builds never call this.
export function connectToDevTools() {}
export default { connectToDevTools };
